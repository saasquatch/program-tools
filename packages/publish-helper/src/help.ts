export const helpText = `
publish-helper --help

SaaSquatch publish helper CLI

publish-helper is a CLI tool for automatically starting CI jobs to publish
packages inside the program-tools repository.

USAGE:

publish-helper [OPTIONS]

OPTIONS:

    --dry-run
        Print the \`gh\` CLI commands instead of executing them

    --help
        Print this help text
`;
