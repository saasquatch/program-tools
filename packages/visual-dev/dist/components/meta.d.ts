/// <reference types="react" />
interface VisualSpecProps {
    spec: string;
    onModal?: boolean;
}
export declare const VisualSpec: ({ spec, onModal }: VisualSpecProps) => JSX.Element;
export {};
