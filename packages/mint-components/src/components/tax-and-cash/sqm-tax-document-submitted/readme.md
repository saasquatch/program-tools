# sqm-tax-document-submitted



<!-- Auto Generated Below -->


## Properties

| Property                          | Attribute                            | Description                                                                       | Type     | Default                                    |
| --------------------------------- | ------------------------------------ | --------------------------------------------------------------------------------- | -------- | ------------------------------------------ |
| `badgeTextAwaitingReview`         | `badge-text-awaiting-review`         | Description text which appears beside badge showing the form is awaiting review   | `string` | `defaults.badgeTextAwaitingReview`         |
| `badgeTextExpiredOn`              | `badge-text-expired-on`              | Description text which appears beside badge showing the form has expired          | `string` | `defaults.badgeTextExpiredOn`              |
| `badgeTextExpiringSoon`           | `badge-text-expiring-soon`           | Description text which appears beside badge showing the form expiring soon        | `string` | `defaults.badgeTextExpiringSoon`           |
| `badgeTextSubmittedOn`            | `badge-text-submitted-on`            | Description text which appears beside badge showing when the form was submitted   | `string` | `defaults.badgeTextSubmittedOn`            |
| `bankingInformationSectionHeader` | `banking-information-section-header` | Header text displayed above the banking information card                          | `string` | `defaults.bankingInformationSectionHeader` |
| `generalErrorDescription`         | `general-error-description`          | The error message shown at the top of the page in an error banner                 | `string` | `defaults.error.generalDescription`        |
| `generalErrorTitle`               | `general-error-title`                | The title for error message shown at the top of the page in an error banner       | `string` | `defaults.error.generalTitle`              |
| `invalidForm`                     | `invalid-form`                       | Description text displayed next to the badge in the tax documents status          | `string` | `defaults.invalidForm`                     |
| `newFormButton`                   | `new-form-button`                    | Text displayed in the submit new document button at the bottom of the page        | `string` | `defaults.newFormButton`                   |
| `noFormNeededSubtext`             | `no-form-needed-subtext`             | Subtext displayed at the bottom of the page if there are no tax documents to show | `string` | `defaults.noFormNeededSubtext`             |
| `statusTextActive`                | `status-text-active`                 | Status text displayed in badge when tax document is Active                        | `string` | `defaults.statusTextActive`                |
| `statusTextExpired`               | `status-text-expired`                | Status text displayed in badge when tax document is Expired                       | `string` | `defaults.statusTextExpired`               |
| `statusTextNotActive`             | `status-text-not-active`             | Status text displayed in badge when tax document is Not Active                    | `string` | `defaults.statusTextNotActive`             |
| `statusTextNotVerified`           | `status-text-not-verified`           | Status text displayed in badge when tax document is Not Verified                  | `string` | `defaults.statusTextNotVerified`           |
| `taxAlertHeaderExpiredOn`         | `tax-alert-header-expired-on`        | Header displayed in alert header at the top of the page.                          | `string` | `defaults.taxAlertHeaderExpiredOn`         |
| `taxAlertHeaderExpiringSoon`      | `tax-alert-header-expiring-soon`     | Header displayed in alert header at the top of the page.                          | `string` | `defaults.taxAlertHeaderExpiringSoon`      |
| `taxAlertHeaderNotActive`         | `tax-alert-header-not-active`        | Header displayed in alert header at the top of the page.                          | `string` | `defaults.taxAlertHeaderNotActive`         |
| `taxAlertMessage`                 | `tax-alert-message`                  | Description text displayed in alert body at the top of the page.                  | `string` | `defaults.taxAlertMessage`                 |
| `taxAlertMessageExpiringSoon`     | `tax-alert-message-expiring-soon`    | Description text displayed in alert body at the top of the page.                  | `string` | `defaults.taxAlertMessageExpiringSoon`     |
| `taxDocumentSectionHeader`        | `tax-document-section-header`        | Header text displayed above the tax documents status                              | `string` | `defaults.taxDocumentSectionHeader`        |
| `taxDocumentSectionSubHeader`     | `tax-document-section-sub-header`    | Sub header text displayed above the tax documents status                          | `string` | `defaults.taxDocumentSectionSubHeader`     |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
