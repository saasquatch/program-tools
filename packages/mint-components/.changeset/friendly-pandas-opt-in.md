---
"@saasquatch/mint-components": patch
---

Add `checkboxOptional` prop to `sqm-marketing-emails-checkbox` so the marketing emails opt-in checkbox can be configured as optional instead of always required.
